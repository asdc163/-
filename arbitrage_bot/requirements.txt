# ============================================================
# Hyperliquid × Aster Arbitrage Bot — Requirements
# Python 3.11+
# ============================================================

# --- Hyperliquid official SDK ---
# Provides Exchange (order placement) and Info (market data) clients.
hyperliquid-python-sdk>=0.9.0

# --- EVM account (HL uses EIP-712 wallet signing) ---
eth-account>=0.11.0

# --- Async HTTP (Aster REST API + Telegram) ---
aiohttp>=3.9.3

# --- WebSocket (HL live data feed) ---
websockets>=12.0

# --- Config / YAML parsing ---
PyYAML>=6.0.1

# --- Credential encryption (Fernet / AES-128-CBC) ---
cryptography>=42.0.0

# --- Structured logging ---
structlog>=24.1.0

# --- Env-var support (.env file loading) ---
python-dotenv>=1.0.0
