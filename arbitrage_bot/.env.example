# Copy this file to .env and fill in the real password.
# .env is git-ignored — never commit your real password.
#
# The password is used to decrypt the credentials in config.yaml.
# It was set when you ran: python utils/key_encryptor.py
ARB_KEY_PASSWORD="replace_with_your_strong_password"
